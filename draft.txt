==可以进行类型转换
===需要严格相等

??是空值合并运算符，当左侧是 `null` 或 `undefined` 时返回右侧值。

三元运算符 “condition ? a: b” 当条件为 `false` 时返回 `b`

没有break，switch会一直执行到最后
（switch：case		if elif else		do-while）
“break”＋循环名可以退出对应循环
循环名：for{。。。。。。}
break 循环名；
continue跳过当前位置的后续代码

逻辑运算符and or not（反转结果）

i++先取用后加1，++i先加1后取用

数组：push加入一个值，pop在末尾剔除一个值，shift移除首元素，slice返回子数组，splice删除并插入元素。


List：C's array + ；linked list
resizable,dynamic
let a=[];
let b=[1,3,4];
let c=new Array(10);//[null,null,null,......]

add on tail: a.push(2)
remove from tail:let y =a.pop()
